# Copy to .env in project root (restart Vite after changes)

# Backend API URL — must match backend PORT (default 5000)
VITE_API_URL=http://localhost:5000

# Optional: Groq for AI Study Plan page
VITE_GROQ_API_KEY=
